import type { Messages } from "@/lib/i18n";

export default function AggregatedAlerts({ articles, messages }: { articles: Array<{ id: string; title: string; summary: string | null; canonicalUrl: string | null; sourceName: string | null; publishedAt: Date | null; category: string; reliability: number | null }>; messages: Messages }) {
  if (articles.length === 0) return null;
  return <section className="mt-8 rounded-2xl border border-slate-200 bg-slate-50 p-5 sm:p-6"><div className="flex items-center justify-between gap-3"><h2 className="text-xl font-semibold text-slate-900">{messages.aggregation.alertsTitle}</h2><span className="text-xs font-semibold uppercase tracking-wide text-emerald-700">{messages.aggregation.external}</span></div><div className="mt-4 grid gap-4 md:grid-cols-2">{articles.map((article) => <article key={article.id} className="rounded-xl border border-slate-200 bg-white p-4"><div className="flex flex-wrap items-center justify-between gap-2"><span className="rounded-full bg-emerald-50 px-2.5 py-1 text-xs font-semibold text-emerald-700">{article.category}</span>{article.reliability !== null && <span className="text-xs font-semibold text-slate-500">{messages.aggregation.reliability}: {article.reliability}/5</span>}</div><h3 className="mt-3 font-semibold text-slate-900">{article.title}</h3>{article.summary && <p className="mt-2 text-sm leading-6 text-slate-600">{article.summary}</p>}<p className="mt-3 text-xs text-slate-400">{article.sourceName} · {article.publishedAt?.toLocaleDateString()}</p>{article.canonicalUrl && <a href={article.canonicalUrl} target="_blank" rel="noopener noreferrer" className="mt-4 inline-flex text-sm font-semibold text-emerald-700">{messages.aggregation.officialPortal}</a>}</article>)}</div></section>;
}
