import type { Messages } from "@/lib/i18n";

export default function AggregatedScholarships({ scholarships, messages, locale }: { scholarships: Array<{ id: string; title: string; provider: string; country: string; deadline: Date; applicationUrl: string; canonicalUrl: string | null; sourceName: string | null }>; messages: Messages; locale: "fr" | "en" }) {
  if (scholarships.length === 0) return null;
  return <section className="mt-8 rounded-2xl border border-slate-200 bg-white p-5 sm:p-6"><div className="flex flex-wrap items-center justify-between gap-3"><h2 className="text-xl font-semibold text-slate-900">{messages.aggregation.scholarshipsTitle}</h2><span className="rounded-full bg-sky-50 px-2.5 py-1 text-xs font-semibold text-sky-700">{messages.aggregation.external}</span></div><div className="mt-4 grid gap-3 md:grid-cols-2">{scholarships.map((item) => <article key={item.id} className="rounded-xl border border-slate-100 bg-slate-50 p-4"><p className="text-xs font-semibold text-slate-500">{item.provider} · {item.country}</p><h3 className="mt-2 font-semibold text-slate-900">{item.title}</h3><p className="mt-2 text-xs text-red-700">{messages.campus.deadline}: {item.deadline.toLocaleDateString(locale === "fr" ? "fr-FR" : "en-US")}</p><a href={item.applicationUrl} target="_blank" rel="noopener noreferrer" className="mt-3 inline-flex text-sm font-semibold text-emerald-700">{messages.aggregation.officialPortal}</a></article>)}</div></section>;
}